
services:

chatbot-api:

```
build: .

container_name: mental-health-chatbot

restart: unless-stopped

ports:
  - "8015:8015"

env_file:
  - .env

volumes:
  - .:/app

command: >
  uvicorn app.main:app
  --host 0.0.0.0
  --port 8015
```

networks:

default:
name: chatbot-network
